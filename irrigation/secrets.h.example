#pragma once
// =============================================================================
// secrets.h.example — template for Big Drippa credentials.
//
// HOW TO USE:
//   1. Copy this file to "secrets.h" (same folder).
//   2. Fill in your values.
//   3. Uncomment the relevant block when you start Phase 2 (WiFi + Firebase).
//
// secrets.h is gitignored — never commit real credentials. Everything below is
// commented out so the Phase 1 (local, no-network) build never references it.
// =============================================================================

// ---- Phase 2: WiFi ----------------------------------------------------------
// #define WIFI_SSID      "your-wifi-name"
// #define WIFI_PASSWORD  "your-wifi-password"

// ---- Phase 2: Firebase Realtime Database ------------------------------------
// Google Sign-In auth, per-UID rules. Channels:
//   /commands  — web app writes, ESP32 reads
//   /history   — ESP32 writes telemetry (last run, duration, trigger source)
// #define FIREBASE_API_KEY        "your-web-api-key"
// #define FIREBASE_DATABASE_URL   "https://your-project-default-rtdb.firebaseio.com/"
// #define FIREBASE_USER_EMAIL     "device-account@example.com"
// #define FIREBASE_USER_PASSWORD  "device-account-password"
